{
  "name": "cloudicons-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:api": "cd api && npm install && npm run build",
    "build:web": "cd cloudicons-web && npm install && npm run build",
    "build:all": "npm run build:api && npm run build:web",
    "dev:api": "cd api && npm run dev",
    "dev:web": "cd cloudicons-web && npm run dev",
    "start": "cd api && npm start"
  }
}
